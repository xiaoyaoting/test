declare type RecoverFn = () => object;
declare type LoadFn = (dbPath: string, options: any) => void;
declare type CloseFn = (dbPath: string) => void;
declare type Backup = {
    recover: RecoverFn;
    load: LoadFn;
    close: CloseFn;
};
export declare function createBackup(dataDir: string): Backup;
export {};
